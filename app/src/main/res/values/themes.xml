<resources>

    <!--Final, top-level theme-->
    <style name="Theme.Apps.DayNight" parent="Theme.Apps" />

    <style name="Theme.Apps" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Apps.AppBarOverlay" parent="ThemeOverlay.Material3.Dark" />

    <style name="Theme.Apps.PopUpOverlay" parent="ThemeOverlay.Material3.Light" />
</resources>